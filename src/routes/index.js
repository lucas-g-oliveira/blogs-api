const login = require('./loginRoutes');
const user = require('./userRoutes');
const post = require('./postRoutes');
const categories = require('./categorieRoutes');

module.exports = { login, user, post, categories };