const categoryControler = require('./categoriesControler');
const loginControler = require('./loginControler');
const postControler = require('./postControler');
const userControler = require('./userControle');

module.exports = {
  categoryControler,
  loginControler,
  postControler,
  userControler,
};