const getAllPost = async (req, res) =>
  res.status(200).send('getAllPosts não implementado');

const getPostById = async (req, res) =>
  res.status(200).send('getPostById não implementado');

const addPost = async (req, res) =>
  res.status(200).send('addPost não implementado');

const deletePostById = async (req, res) =>
  res.status(200).send('deletePostById não implementado');

module.exports = { getAllPost, getPostById, addPost, deletePostById };